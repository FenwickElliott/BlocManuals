[selector]{   //http://www.w3schools.com/cssref/css_selectors.asp
    [property]: [value];
}

element {

}

.class{

}

#id{

}

@media ([min-width: 640px]){
    html .class #id{
        color: blue
    }
}

[.eg] [p]{} /* will select only the paragraph in the class="eg"*/
input[type=...]{} /* styles input areas of forms*/
.[eg]:hover{}

properties:
    text:
        color:
        text-decoration:
        font-size:
        font-family:
        text-align:
        text-transform:
        font:
        font-weight:
        font-style:
        line-height:
    box model:
        width:
        height:
        padding: [top right bottom left]
        border: [width style color]
        margin: [top right bottom left]
        display:
        float:
        transform:
        background: [color image position repeat]


box model styling:
    see: 1.2FrontedFormation.pdf from slide 224 onward

p {
    display: block; //default
}

display:
    inline // each element is displayed on the same line while space allows
    block //each element is displayed on a new line under the previous
    flex
    table
    table-cell etc...
