Objects:
    window
    event listeners

Callback Functions:

Event Handlers:

getAttribute('exampleAttribute')

Properties:
    window.innerHeight
    getBoundingClientRect

NodeList:
    Does not inherit from Array.prototype
    Access:
        document.querySelectorAll("p")[0];          // preferred
        document.querySelectorAll(".eg").item(0);     // slower


Selectors:
    document.querySelector()            // First element node
    document.querySelectorAll()[]         // Static NodeList
    document.getElementById()[]           // Referenced element
    document.getElementsByClassName()[]   // Live NodeList
    document.getElementsByTagName()[]     // Live NodeList
    document.getAttribute('exampleAttribute')

    suffixes:
        .firstChild
        .lastChild
        .childNodes
        .nodeValue
        .textContent
        .innerText
        .innerHTML
        .outerHTML
